%% Memory planning / buffer reuse (polished)
flowchart LR
  %% Using concise nodes to indicate lifetimes
  L0["lin start:0 end:0"] --> L1["lin_relu start:1 end:1"] --> L2["out start:2 end:2"]

%% Caption: Planner computes lifetimes and reuses buffers when live ranges
%% do not overlap to reduce peak memory.
